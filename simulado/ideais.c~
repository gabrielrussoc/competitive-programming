#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#define MAX 10000


int main(){

	int a,b;

	while(scanf("%d %d",&a,&b) != EOF){
		if(a%b == 0 || b%a == 0)
			printf("Y\n");
		else
			printf("N\n");
	}

	return 0;
}
