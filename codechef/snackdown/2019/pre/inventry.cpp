#include <bits/stdc++.h>
#define pb push_back
using namespace std;
typedef long long ll;

const int N = 100005;

int t, n;
char s[N];

int main() {
    scanf("%d", &t);
    while(t--) {
        scanf("%d", &n);
        scanf(" %s", s);

        int i = 0;
        while(i < n && s[i] == '#') i++;
    }

}

